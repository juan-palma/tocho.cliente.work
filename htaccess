<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Evita reescribir si ya es un archivo o directorio existente
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Redirige todo a la carpeta public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>